apiVersion: v1
kind: Service
metadata:
  name: telemetryx-app
  namespace: sdsmcv-prd
  labels:
    app.kubernetes.io/name: telemetryx-app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: telemetryx-app
  ports:
    - name: http
      port: 80
      targetPort: 3000
